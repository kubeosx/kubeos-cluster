apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: dotday
  namespace: dev
  labels:
    backstage.io/kubernetes-id: "dotday"
    backstage.io/kubernetes-namespace: "dev"
  annotations:
    backstage.io/kubernetes-id: "dotday"
    backstage.io/kubernetes-namespace: "dev"
spec:
  interval: 1m0s
  ref:
    branch: main
  url: https://github.com/kubeosx/dotday
